{
  "name": "pwa-test",
  "private": true,
  "description": "",
  "version": "0.1.0",
  "license": "0BSD",
  "scripts": {
    "gatsby": "gatsby",
    "gh-pages": "gh-pages",
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && gh-pages -b prod -d public -r https://$GITHUB_TOKEN@github.com/malmas18/pwa-test",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "2.1.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/malmas18/pwa-test"
  },
  "bugs": {
    "url": "https://github.com/malmas18/pwa-test/issues"
  }
}
